# Backlog

Складываем идеи и недоделки по харнессу. Формат: что хочется, зачем, чем проверять.

**Принцип:** свои расширения не пишем — берём готовые. Если готового нет, задача либо откладывается, либо закрывается настройкой/патчем, а не кодом.

## Открытые задачи

- [ ] **Скиллы.** В `agent/settings.json` прописан путь `~/.agents/skills` и исключения; сам каталог пуст/переехал. Актуализировать путь или убрать блок, чтобы не путаться.
- [ ] **Дефолт `status` у `pi-live-throughput`.** После рестарта режим отображения возвращается к `widget` (строка над редактором); состояние живёт только в памяти сессии, настроек у расширения нет. Вариант без кода — патч одной строки в установленном файле (`let mode: DisplayMode = "widget"` → `"status"`), но слетит при обновлении пакета. Решать, нужно ли это вообще.
- [ ] **CC-вид: рамки и деревья.** В `pi-claude-code-ui` рамка вокруг юзер-сообщения и коннекторы `├ └ │` захардкожены. Своё расширение-компаньон не пишем — если найдётся готовое расширение/тема/настройка, ставим; иначе оставляем как есть.
- [ ] **Проверить дублирование инструкций** между `~/.pi/agent/AGENTS.md` и `~/.agents/AGENTS.md` (второй лежит рядом со скиллами).
- [ ] **Компакция на 1M-контексте.** Проверить, не режет ли `compaction.reserveTokens 16384` / `keepRecentTokens 20000` слишком агрессивно на длинных сессиях с `deepseek-flash`.

## Исследования (без кода)

- Сравнить `pi-live-throughput` с `pi-tokometer` и `@pi-plugins/speed` по точности TTFT и avg TPS на reasoning-модели (`thinking max`) — текущие замеры сделаны на синтетических событиях, а не на живых ответах.
- Таблица «thinking-уровень → реальный расход thinking-токенов» для `deepseek-flash` (по умолчанию включён `max`).
- Проверить, не появилось ли у `pi-claude-code-ui` настроек для формы тул-строк в новых версиях (в 1.0.83 их нет).

## Мёртвые настройки (не тратить время)

В `pi-claude-code-ui` 1.0.83 объявлены, но нигде не читаются: `readOutputMode`, `searchOutputMode`, `bashOutputMode`, `showTruncationHints`. Из `~/.pi/settings.json` убраны.

## Сделано

- Компактный вывод тулов: свёрнутый bash — одна строка, вывод только по `Ctrl+O` (замеры в `compact-output.md`).
- Скрытие thinking-блоков: `Thinking…` во время, `Thought for Ns` после, содержимого нет.
- Тема `claude-green`: Claude Code-подобные нейтральные фоны + зелёный акцент вместо рыжего.
- `pi-live-throughput`: TPS / avg TPS / TTFT / peak / input / cache read.
